import styled from 'styled-components'

const Date = styled.div`
  text-align: center;
  font-size: 0.8rem;
  font-variant: small-caps;
  font-family: 'Helvetica';
  margin: ${p => p.margin || '3rem'};
`

export default Date
