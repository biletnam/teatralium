import styled from 'styled-components';

const Wrapper = styled.div`
  width: 100%;
  position: relative;
  color: white;
  background: black;
  padding: 2rem;
  font-size: 1.4rem;
`;

export default Wrapper;
